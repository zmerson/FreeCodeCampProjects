//const items = ['item1','item2']; 
//we can export and declare the variables in the same line
module.exports.items = ['item1', 'item2']

const person = { 
    name: 'bob',
}

//creates a property & sets the value on the same line that exports it
module.exports.singlePerson = person; 
